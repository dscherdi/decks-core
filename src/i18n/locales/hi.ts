import { en, type Translations } from "./en";

// STUB — Hindi translation pending. Currently falls back to English.
// To translate: copy the shape of de.ts / es.ts and translate each value.
// A native Hindi speaker should review before release.
export const hi: Translations = en;
